Url to access the website: https://julienlecoq.github.io/JulienLecoq

[] Make String() use a computed variable in the User struct to improve performance (avoid re-computation of capitalized first name etc).
